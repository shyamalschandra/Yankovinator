[package]
name = "yankovinator-tui"
version = "0.2.0"
edition = "2021"
description = "Ncurses-like batch progress TUI: color boxes + emoji bars per worker thread"

[dependencies]
crossterm = "0.28"
ratatui = "0.29"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
