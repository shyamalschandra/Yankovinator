[package]
name = "yankovinator-tui"
version = "0.1.0"
edition = "2021"
description = "Fixed-screen batch progress TUI for yankovinator (UTF-8, color, emoji)"

[dependencies]
crossterm = "0.28"
ratatui = "0.29"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
